const express = require('express');
const db = require('./db');
const path = require('path')
const app = express();


app.get('/', (res, req, next) => {

});

app.get('/api/pages', (res, req, next) => {

});

app.get('/api/pages/:id/children', (res, req, next) => {

});

app.get('api/pages/:id/siblings', (res, req, next) => {

});
